import { View, Text } from "react-native";

import { DetailLayout } from "@/components/common/layout";

export const Menu = () => {
    return <DetailLayout>
        <Text>Menu</Text>
    </DetailLayout>
}